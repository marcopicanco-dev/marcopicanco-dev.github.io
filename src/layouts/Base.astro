---
import HeadSeo from '@/components/HeadSeo.astro'
import Nav from '@/components/navigation/Nav.astro'
import { DEFAULT_LANGUAGE_CODE } from '@/config'
import { type LanguageKeys, LANGUAGES } from '@/i18n/ui'
import { type Frontmatter } from '@/types'

import karla400Url from '@fontsource/karla/files/karla-latin-400-normal.woff2?url'
import karla500Url from '@fontsource/karla/files/karla-latin-500-normal.woff2?url'
import karla700Url from '@fontsource/karla/files/karla-latin-700-normal.woff2?url'
import '@fontsource/karla/latin-400.css'
import '@fontsource/karla/latin-500.css'
import '@fontsource/karla/latin-700.css'

// custom styling
import '@/styling/tailwind.css'
import '@/styling/scrollbar.css'
import '@/styling/code.css'

const { frontmatter } = Astro.props as {
  frontmatter?: Frontmatter
}

const langFromURL = Astro.url.pathname.split('/')[1] || DEFAULT_LANGUAGE_CODE
const lang: LanguageKeys = Object.prototype.hasOwnProperty.call(
  LANGUAGES,
  langFromURL,
)
  ? (langFromURL as LanguageKeys)
  : DEFAULT_LANGUAGE_CODE
---

<style is:global>
  body {
    font-family: 'Karla', sans-serif;
  }
</style>

<html data-theme="dracula" lang={lang}>
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <meta name="referrer" content="strict-origin-when-cross-origin" />
    <link
      rel="preload"
      href={karla400Url}
      as="font"
      type="font/woff2"
      crossorigin
    />
    <link
      rel="preload"
      href={karla500Url}
      as="font"
      type="font/woff2"
      crossorigin
    />
    <link
      rel="preload"
      href={karla700Url}
      as="font"
      type="font/woff2"
      crossorigin
    />
    <HeadSeo type="website" frontmatter={frontmatter} />
  </head>
  <body
    class="bg-lightModeBg text-lightModeText selection:bg-draculaAccent/30 selection:text-draculaBg dark:bg-darkModeBg dark:text-darkModeText"
  >
    <Nav />
    <div
      class="fixed left-0 top-0 z-[-2] h-screen w-full bg-lightModeBg bg-[radial-gradient(circle_at_top_right,_rgba(189,147,249,0.2),_transparent_25%),radial-gradient(circle_at_bottom_left,_rgba(139,233,253,0.12),_transparent_30%)] transition-colors duration-[inherit] dark:bg-darkModeBg dark:bg-[radial-gradient(circle_at_top_right,_rgba(189,147,249,0.15),_transparent_25%),radial-gradient(circle_at_bottom_left,_rgba(139,233,253,0.08),_transparent_30%)]"
    >
    </div>
    <div class="pt-20">
      <slot />
    </div>
  </body>
</html>

<script>
  import {
    LIGHT_MODE_CODE_BLOCK_THEME,
    DARK_MODE_CODE_BLOCK_THEME,
  } from '@/config'

  function init() {
    document.documentElement.classList.add('scroll-smooth')
    // adding scroll-smooth class to html tag manually didn't work so I'm adding that class this way

    if (
      localStorage.theme === 'dark' ||
      (!('theme' in localStorage) &&
        window.matchMedia('(prefers-color-scheme: dark)').matches)
    ) {
      document.documentElement.classList.add('dark')
      document.documentElement.setAttribute(
        'data-theme',
        DARK_MODE_CODE_BLOCK_THEME as string,
      )
    } else {
      document.documentElement.classList.remove('dark')
      document.documentElement.setAttribute(
        'data-theme',
        LIGHT_MODE_CODE_BLOCK_THEME as string,
      )
    }

    setTimeout(() => {
      document.body.classList.add('duration-300', 'transition-colors')
    }, 300)

    // im adding those classes with 300ms delay, so theres no flashing on inital page load
  }

  function setupEmailLinks() {
    const links = document.querySelectorAll('[data-email-link]')

    links.forEach((link) => {
      if (!(link instanceof HTMLAnchorElement)) return

      const { emailUser, emailDomain } = link.dataset

      if (!emailUser || !emailDomain) return

      const scheme = ['mai', 'lto:'].join('')

      link.href = `${scheme}${emailUser}@${emailDomain}`
    })
  }

  init()
  setupEmailLinks()
  document.addEventListener('astro:after-swap', init)
  document.addEventListener('astro:after-swap', setupEmailLinks)
</script>
